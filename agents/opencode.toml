# opencode screen manifest. Untested locally.

name = "opencode"
processes = ["opencode"]

detect = [
  'opencode v',
  'opencode --',
  'OpenCode',
]

[[rules]]
name = "blocked-approval"
state = "blocked"
any = [
  'Do you want to',
  'Allow\b.*\?',
  '❯\s*1\.\s*Yes',
  '\[y/n\]',
]

[[rules]]
name = "working"
state = "working"
within = 4
any = [
  'esc to interrupt',
  '\bWorking\b',
]

[[rules]]
name = "idle-prompt"
state = "idle"
any = ['^\s*│\s*>', '^\s*>\s*$']
